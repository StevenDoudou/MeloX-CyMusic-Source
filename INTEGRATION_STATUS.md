# Integration status

- MeloX base snapshot copied from the current upstream-derived project.
- CyMusic source integration skeleton lives under `MeloXSource/`.
- GitHub Actions unsigned IPA workflow is included.
- The Swift files are kept as a separate source group until Xcode target membership and runtime protocol tests are complete.
